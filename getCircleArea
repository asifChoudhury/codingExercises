/*
    Given a radius, write a function that calculates the area of a circle.
*/

getCircleArea = (radius) => {
    console.log(`Given, radius = ${radius}.`);

    let area;
    area = Math.PI * Math.pow(radius, 2);

    console.log(`The area is ${area}`);
}
getCircleArea(3);
